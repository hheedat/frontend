<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta charset="UTF-8">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <header>
  	<h1><%= title %></h1>
  </header>
  <nav>
  	<span><a href="/">主页</a></span>
    <% if(user){ %>
        <span><a href="/post">post</a></span>
        <span><a href="/logout">logout</a></span>
    <% } else { %>
        <span><a href="/login">登录</a></span>
        <span><a href="/reg">注册</a></span>
    <% } %>
  </nav>
  <article>
    <% if(success){ %>
      <div class="prompt-box"><%= success %></div>
    <% } %>
     <% if(error){ %>
      <div class="prompt-box"><%= error %></div>
    <% } %>